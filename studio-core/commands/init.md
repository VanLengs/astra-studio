---
description: Initialize plugin development studio in this project
---

Initialize the `studio/` directory for plugin development. Creates a git-tracked workspace with config, changes, and archive directories.

Use skill: "init"
