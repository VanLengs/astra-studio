---
description: Implement an OpenSpec change proposal as working React/Next.js code
argument-hint: [change-id]
---

Read the OpenSpec change proposal `$ARGUMENTS` and implement it as working code.

If no argument is provided, list active changes in `openspec/changes/` and ask the user to choose.

Use skill: "openspec-to-code"
