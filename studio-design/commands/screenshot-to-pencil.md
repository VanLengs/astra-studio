---
description: Convert a UI screenshot into a Pencil (.pen) prototype file
argument-hint: [screenshot path]
---

Analyze the screenshot at `$ARGUMENTS` and generate a Pencil (.pen) prototype file.

If no argument is provided, ask the user to provide a screenshot (file path or paste an image).

Use skill: "screenshot-to-pencil"
