---
description: Convert a Pencil (.pen) prototype into an OpenSpec change proposal
argument-hint: [.pen file path]
---

Parse the Pencil prototype at `$ARGUMENTS` and generate an OpenSpec change proposal with requirements, scenarios, and implementation tasks.

If no argument is provided, search for `.pen` files in `docs/design/`.

Use skill: "pencil-to-openspec"
