---
description: Configure MCP server connections for a plugin
argument-hint: [plugin-directory-path]
---

Scan the plugin at `$ARGUMENTS` for MCP requirements, match to available servers, and generate a validated `.mcp.json` configuration.

Use skill: "mcp-wiring"
