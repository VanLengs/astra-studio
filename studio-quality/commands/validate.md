---
description: Validate a plugin for structural correctness and compliance
argument-hint: [plugin-directory-path]
---

Run comprehensive validation on the plugin at `$ARGUMENTS`. Checks manifest schema, skill quality, dependencies, and MCP configuration. Produces a pass/fail report with remediation steps.

Use skill: "plugin-validator"
